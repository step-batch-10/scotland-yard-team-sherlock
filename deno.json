{
  "imports": {
    "testing": "jsr:@std/testing@^1.0.11",
    "assert": "jsr:@std/assert@^1.0.12",
    "hono": "npm:hono@^4.7.6"
  },
  "tasks": {
    "start": "deno run -A --watch server.ts",
    "test": "deno test -A",
    "lint": "deno lint",
    "format": "deno fmt",
    "test-coverage": "deno test -A --coverage=coverage --clean",
    "coverage": {
      "command": "deno coverage --html && deno run -A jsr:@ashishkujoy/deno-coverage-checker@0.0.8",
      "dependencies": ["test-coverage"]
    },
    "prod": "deployctl deploy --project=scotland-yard-production --entrypoint=server.ts --org='Team Sherlock'",
    "qa": "deployctl deploy --project=scotland-yard-qa-16 --entrypoint=server.ts --org='Team Sherlock'"
  },
  "lint": {
    "rules": {
      "tags": ["recommended"],
      "include": ["camelcase", "eqeqeq", "default-param-last", "no-console"],
      "exclude": []
    }
  },

  "fmt": {
    "indentWidth": 2,
    "semiColons": true
  },
  "test": {
    "include": ["**/*.ts"],
    "exclude": ["server.ts"]
  }
}
